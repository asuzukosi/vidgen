Write a {{ segment.duration }}-second voiceover script for an explainer video segment.

Video Title: {{ video_title }}
Segment {{ segment_num }}/{{ total_segments }}: {{ segment.title }}
Purpose: {{ segment.purpose | default('Explain this topic') }}

Key Points to Cover:
{% if key_points_text %}
{{ key_points_text }}
{% else %}
- {{ segment.purpose | default('Main concept') }}
{% endif %}

Guidelines:
1. Write in a conversational tone
2. Aim for approximately {{ (segment.duration * 2.5) | round(0) | int }} words (based on ~150 words/minute speaking rate)
3. Start naturally - {% if is_intro %}introduce the topic{% else %}discuss the topic based on the segment's purpose and key points{% endif %}
4. {% if is_conclusion %}End with a strong conclusion or call-to-action{% else %}End by closing the points made in the segment{% endif %}
5. Use simple, clear language
6. Make it engaging and memorable

Write the script now:
