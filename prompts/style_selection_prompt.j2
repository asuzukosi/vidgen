Analyze this video segment and select the best visual style.

Segment {{ segment_num }}: {{ segment.title }}

Purpose: {{ segment.purpose | default('') }}

Key Points:
{% for point in (segment.key_points | default([]))[:3] %}
- {{ point }}
{% endfor %}

Visual Keywords: {{ (segment.visual_keywords | default([]))[:5] | join(', ') }}

Available Images:
- PDF images: {{ (segment.pdf_images | default([])) | length }}
- Stock images: {% if segment.stock_image %}Yes{% else %}No{% endif %}

Select ONE style and provide a brief reason:

Format your response EXACTLY as:
STYLE: [slideshow/animated/ai_generated]
REASON: [one sentence explaining why this style works best]

Choose now:

